import { Button } from "./ui/button";

const Header = () => {
    return (
        <header>
            <Button>Click me</Button>

            <h1>Header</h1>
        </header>
    );
};

export default Header;
